{
    "version":2,
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
    "builds":[
        {"src":"*.ts", "use":"@vercel/node"}
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"/app.ts"
        }
    ]
}